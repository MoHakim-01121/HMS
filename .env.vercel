# Vercel Environment Variables
# Add these in Vercel Dashboard: Settings > Environment Variables

# Security
SECRET_KEY=your-secret-key-50-characters-minimum-change-this
DEBUG=False

# Hosts (add your Vercel domain)
ALLOWED_HOSTS=.vercel.app,yourdomain.com

# Database (use Vercel Postgres or other)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: Admin URL (hide admin panel)
ADMIN_URL=secret-admin-path/
